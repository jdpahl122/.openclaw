# Gmail account to monitor (must match gog auth setup)
GOG_ACCOUNT=you@gmail.com

# Path to gog binary (auto-detected if on PATH)
GOG_BIN=gog

# How far back to look on first run (hours)
INITIAL_LOOKBACK_HOURS=24

# Log level (DEBUG, INFO, WARNING, ERROR)
EMAIL_MONITOR_LOG_LEVEL=INFO
